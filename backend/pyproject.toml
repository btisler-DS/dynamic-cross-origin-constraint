[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "project-synapse"
version = "0.1.0"
description = "MARL simulation harness for The Inevitable Calculus of Language"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
